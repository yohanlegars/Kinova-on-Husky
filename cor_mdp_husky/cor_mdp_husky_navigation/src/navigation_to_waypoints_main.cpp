#include "navigation_to_waypoints.hpp"

int main(int argc, char **argv)
{
    ros::init(argc, argv, "navigation_to_waypoints");

    navigation_to_waypoints navnode ;//= navigation_waypoints();


    // ros::Rate rate(60); // Hz rate
    // while (ros::ok())
    // {
    //     // Let Ros take over
    //     ros::spinOnce();
    //     rate.sleep();
    // }
}